import React from 'react';
import IntegracionesPage from '@/pages/pyme/integraciones/IntegracionesPage';

// Wrapper to render the comprehensive Integration/Customization page
// This unifies the previously separate /integracion page with the tenant-scoped version.
const Integracion = () => {
  return <IntegracionesPage />;
};

export default Integracion;
